export type ButtonType = {
  className?: string;
  children: React.ReactNode;
};

export type reasonsArrayType = {
  img: string;
  heading: string;
  text: string;
}[];

export type servicesArrayType = {
  img: string;
  heading: string;
  text: string;
}[];

export type worksArrayType = {
  img: string;
  heading: string;
  text: string;
  subheading: string;
  date: string;
}[];

export type TestimonialsArrayType = {
  icon: string;
  comment: string;
  userName: string;
  userTitle: string;
  userImage: string;
}[];

export type FAQArrayType = {
  title: string;
  text: string;
}[];
